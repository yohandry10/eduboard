---
import Layout from '../../layouts/Layout.astro';
---

<Layout title="Proyector Interactivo">

  <!-- Contenedor con degradado en los bordes -->
  <div class="page-gradient">

    <!-- Contenedor central con fondo blanco -->
    <div class="main-content">

      <!-- Hero Section -->
      <div class="max-w-4xl mx-auto text-center mb-16">
        <h1 class="text-4xl font-bold mb-6">Proyector Interactivo</h1>
        <p class="text-xl text-gray-600">
          Convierte cualquier superficie en una pantalla interactiva.
        </p>
      </div>

      <!-- Sección de información del proyector (sin bordes ni sombras) -->
      <div class="bg-white rounded-lg p-8 max-w-2xl mx-auto">
        <div class="grid md:grid-cols-2 gap-8">
          <!-- Columna izquierda: Imagen -->
          <div class="rounded-lg overflow-hidden">
            <img
              src="/proyector-interactivo.webp"
              alt="Proyector Interactivo"
              class="w-full h-auto"
            />
          </div>

          <!-- Columna derecha: Información -->
          <div>
            <h2 class="text-2xl font-bold mb-4">
              Proyector Interactivo Epson BrightLink
            </h2>
            <p class="text-gray-600 mb-6">
              Con sus dos lápices interactivos, el Epson BrightLink resulta muy fácil y cómodo para interactuar con tu proyección. Ofreciendo un nivel de luminosidad de 2.700 lúmenes en blanco y color, además de resolución WXGA, el proyector de ultracorta distancia permite realizar presentaciones eliminando las sombras y destellos en los ojos.
            </p>

            <!-- Precio -->
            <div class="price-box mb-6">
              <p class="text-2xl font-bold">S./5,500</p>
              <p class="text-sm">Incluido IGV</p>
            </div>

            <!-- Botón de acción -->
            <a
              href="/contacto"
              class="contact-button px-6 py-3 rounded-full font-semibold transition-colors text-center block"
            >
              Contáctanos
            </a>
          </div>
        </div>
      </div>

      <!-- Sección de características (sin bordes ni sombras) -->
      <div class="max-w-4xl mx-auto mt-16">
        <h2 class="text-3xl font-bold text-center mb-8">Características Principales</h2>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
          {[
            '2 lápices interactivos incluidos',
            '2.700 lúmenes en blanco y color',
            'Resolución WXGA (1280 x 800)',
            'Distancia ultracorta para evitar sombras',
            'Conexión HDMI y USB',
            'Compatibilidad con Windows y macOS',
            'Fácil instalación y configuración',
            'Garantía de 2 años',
          ].map((feature) => (
            <div class="bg-white p-6 rounded-lg">
              <p class="text-gray-800">✓ {feature}</p>
            </div>
          ))}
        </div>
      </div>

    </div>
    <!-- Fin de .main-content -->

  </div>
  <!-- Fin de .page-gradient -->

  <!-- Estilos adicionales -->
  <style>
    /* 1. Reseteo básico del body */
    body {
      margin: 0;
      padding: 0;
      background: #f5f5f5; 
      color: #333;
      font-family: "Helvetica Neue", Helvetica, Arial, sans-serif;
    }

    /* 2. Fondo degradado en los bordes (consistente con el resto de la web) */
    .page-gradient {
      width: 100%;
      min-height: 100vh;
      background: linear-gradient(135deg, #C02425, #F0CB35);
      padding: 2rem 0;
    }

    /* 3. Contenedor principal con fondo blanco */
    .main-content {
      max-width: 1200px;
      margin: 0 auto;
      background: #fff;
      border-radius: 8px;
      padding: 2rem;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
    }

    /* Caja de precio: fondo negro y texto blanco para consistencia */
    .price-box {
      background: #000;
      color: #fff;
      padding: 1rem;
      border-radius: 8px;
      text-align: center;
      border: 1px solid #ddd;
    }
    
    /* Botón de acción (Contáctanos) */
    .contact-button {
      background: #C02425;
      color: #fff;
      text-decoration: none;
    }
    .contact-button:hover {
      background: #F0CB35;
    }
  </style>

</Layout>
